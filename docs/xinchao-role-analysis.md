# 🗣️ 씬짜오 오늘의 뉴스 — 역할별 관점 분석 회의록

> **회의 목적:** 코드베이스 전체 검토를 통해 역할자별 미흡한 관점과 개선 방안을 도출
> **참석자(가상):** 일반 사용자 A, 광고 고객 B, 앱 CEO C, 앱 관리자 D
> **기준 파일:** `app/page.js`, `app/admin/*`, `lib/*`, `components/AdBanner.js`, `scripts/*`

---

## 💬 1부: 역할별 발언 (현재 미흡한 점)

---

### 👤 일반 사용자 A

> "저는 매일 뉴스를 읽으러 오는데, 솔직히 좀 불편한 점들이 있어요."

**발견된 미흡 사항:**

| # | 항목 | 근거 (코드) |
|---|------|------------|
| 1 | **번역 품질 표시 없음** — AI 번역인지 사람 번역인지 알 수 없음 | `translationStatus` 필드는 DB에만 존재, UI에 미노출 |
| 2 | **"지난 뉴스 보기" 버튼이 동작 안 함** — `<button>` 태그만 있고 링크 없음 | `page.js` L179: 단순 `<button>` (href/router 없음) |
| 3 | **인기 키워드가 하드코딩** — 실제 클릭 트래킹 없이 고정값 | `page.js` L191: `['경제성장', '지하철', 'K-Pop'...]` 정적 배열 |
| 4 | **"많이 본 뉴스"가 실제 조회수 기반이 아님** — 그냥 최신 3개 노출 | `page.js` L200: `displayNews.slice(0, 3)` |
| 5 | **뉴스 공유 기능 없음** — SNS 공유 버튼 없음 | 전체 `page.js`에서 공유 기능 없음 |
| 6 | **이메일 구독 UI가 메인 페이지에 없음** — 구독 유도 없음 | `/api/subscribe` API는 존재하지만 메인 페이지에 노출 안됨 |
| 7 | **카테고리가 비어있으면 섹션이 통째로 사라짐** — 안내 문구 없음 | `page.js` L113: 빈 배열이면 아무것도 렌더링 안됨 |

---

### 📢 광고 고객 B

> "광고를 집행하고 싶은데, 광고 효과가 얼마나 되는지, 제 광고가 잘 노출되는지 전혀 확인할 방법이 없습니다."

**발견된 미흡 사항:**

| # | 항목 | 근거 (코드) |
|---|------|------------|
| 1 | **광고 배너가 껍데기만 존재** — 실제 광고 연동 없음 | `AdBanner.js`: 회색 박스에 "광고 배너 영역" 텍스트만 표시 |
| 2 | **Google Ads 미연동** — 코드에 광고 스크립트 없음 | `layout.js`, `page.js` 어디에도 `googletag`, `adsbygoogle` 없음 |
| 3 | **광고 노출/클릭 통계 없음** — 광고 성과 확인 불가 | `app/admin/stats/page.js`: 광고 관련 지표 전혀 없음 |
| 4 | **광고 문의 채널 없음** — 사이트 내 광고 신청 경로 부재 | 전체 `app/` 디렉토리에 광고 문의 페이지 없음 |
| 5 | **배너 종류/위치 변경 불가** — 관리자가 바꿀 수 없음 | `AdBanner.js`: 하드코딩된 단일 컴포넌트 |
| 6 | **AdBanner가 같은 화면에 연속 2번 렌더링** — 실수로 중복 삽입 | `page.js` L161-163: `<AdBanner />` 두 번 연속 |

---

### 👔 앱 CEO C

> "서비스 운영 현황을 한눈에 보고 싶은데, 비즈니스 관점 지표가 없습니다. 구독자 성장 추이, 수익, 핵심 KPI가 전혀 보이지 않아요."

**발견된 미흡 사항:**

| # | 항목 | 근거 (코드) |
|---|------|------------|
| 1 | **구독자 증가 추이 그래프 없음** — 숫자만 보임 | `subscribers/page.js`: 단순 목록만 있고 성장 차트 없음 |
| 2 | **이메일 발송 성공률/열람률 없음** | `email-service.js`: BCC 발송이라 개인별 추적 불가, 통계 미집계 |
| 3 | **수익 관련 지표 없음** — 광고 수익, 구독자 가치 등 | `admin/stats/page.js`: 수집/발행 건수만 있고 비즈니스 KPI 없음 |
| 4 | **뉴스레터 성과 추이 없음** — 언제 구독자가 늘었는지 알 수 없음 | DB에 `createdAt`은 있으나 대시보드에 시간별 추이 미제공 |
| 5 | **SEO 성과 연동 없음** — 검색 유입, 페이지뷰, 트래픽 없음 | `page.js`: Google Analytics, Search Console 연동 없음 |
| 6 | **공개 사이트 방문자 통계 없음** | `app/admin/stats/page.js`: 내부 뉴스 생산 통계만 있음 |
| 7 | **광고 수익 모델이 정의되지 않음** | `AdBanner.js`가 미완성 상태로 광고 = 0원 |

---

### 🛠️ 앱 관리자 D

> "매일 뉴스를 발행하는데, 오류가 생겼을 때 파악이 어렵고, 반복 작업도 너무 많습니다."

**발견된 미흡 사항:**

| # | 항목 | 근거 (코드) |
|---|------|------------|
| 1 | **인증/접근제어 없음** — `/admin` 경로에 누구나 접근 가능 | `admin/layout.js`: 세션 체크 로직 없음 (`auth.js` 파일은 있으나 admin에 미적용) |
| 2 | **자동 발행 스케줄 없음** — 매일 수동 발행 필요 | `scripts/scheduler.js`만 있고 Cron/자동화 미구현 |
| 3 | **번역 비용 추적 없음** — GPT API 비용이 얼마나 드는지 모름 | `batchTranslateAction()`: 호출 횟수/비용 추적 없음 |
| 4 | **실패 뉴스 재시도 없음** — 발행 실패 시 수동 재처리 필요 | `batchPublishDailyAction()`: 실패 시 에러 로그만 남기고 재시도 없음 |
| 5 | **카테고리 불일치 방치** — 한국어/영어 카테고리가 동시에 존재 | `admin/page.js` L137-170: `"Society"`, `"사회"` 둘 다 처리하는 분기 |
| 6 | **카드뉴스 초기화 버튼이 빈 함수** | `settings/page.js` L22-24: `resetCardNews`가 `// ... (기존 코드)` 주석만 있음 |
| 7 | **WordPress 동기화 실패 시 알림 없음** | `updateWordPressPostMeta()`: 실패 시 `console.warn`만 하고 UI 알림 없음 |
| 8 | **통계에 구독자 수 없음** | `admin/stats/page.js`: 구독자 수가 통계 대시보드에서 분리됨 |

---

## 🔴 2부: 회의 토론 — 우선순위 결정

**참석자들이 공통으로 동의한 최우선 개선 항목:**

### 🥇 긴급 (즉시 수정 필요)

1. **`/admin` 인증 적용** — 현재 보안 공백 (관리자/CEO 공동 요구)
2. **`AdBanner` 연속 중복** 제거 (`page.js` L161-163)
3. **"지난 뉴스 보기" 버튼 기능 구현** 또는 제거
4. **`resetCardNews` 함수 구현** — 현재 껍데기만 있음

### 🥈 단기 (다음 스프린트)

5. **이메일 구독 버튼을 메인 페이지에 추가** — 구독자 확보 핵심
6. **인기 키워드/조회수를 실제 데이터 기반으로** 교체
7. **광고 배너 실제 연동** (Google AdSense 또는 자체 배너 CMS)
8. **번역 비용 추적 로그** 추가 (API 토큰 사용량)

### 🥉 중기 (분기 계획)

9. **CEO용 비즈니스 대시보드** — 구독자 그래프, 이메일 열람률, 트래픽
10. **카테고리 통일** — DB 마이그레이션으로 한국어/영어 혼용 정리
11. **자동 발행 스케줄러** — Vercel Cron으로 매일 자동 발행
12. **실패 재시도 큐** — 발행 실패 뉴스 자동 재처리

---

## ✅ 3부: 결론 및 개선 방안 요약

### 즉시 조치 (코드 수정)

```diff
// 1. page.js - 중복 AdBanner 제거
- <AdBanner />
- <AdBanner />
+ <AdBanner />

// 2. page.js - 지난뉴스 버튼을 실제 링크로
- <button>지난 뉴스 보기</button>
+ <Link href="/news/archive">지난 뉴스 보기</Link>

// 3. admin/layout.js - 인증 미들웨어 추가
+ import { auth } from '@/lib/auth';
+ const session = await auth();
+ if (!session) redirect('/admin/login');
```

### 신규 기능 구현 방향

| 역할 | 핵심 개선 기능 | 예상 임팩트 |
|------|--------------|-----------|
| 일반 사용자 | 메인 페이지 구독 폼, 뉴스 공유 버튼, 조회수 기반 인기 뉴스 | 구독자 증가, 재방문율 향상 |
| 광고 고객 | Google AdSense 연동, 광고 문의 페이지, 광고 관리 CMS | 수익 모델 구축 |
| CEO | 비즈니스 KPI 대시보드, 이메일 열람률 추적, GA4 연동 | 의사결정 지원 |
| 관리자 | Admin 인증 강화, 자동 발행 스케줄러, 번역 비용 모니터링 | 운영 효율화 |

---

> **회의 결론:** 현재 시스템은 뉴스 수집·번역·발행 파이프라인은 잘 구축되어 있으나,  
> **① 사용자 인게이지먼트, ② 광고 수익화, ③ 보안, ④ 비즈니스 지표** 네 영역이 미완성 상태입니다.  
> 가장 먼저 `/admin` 보안과 `AdBanner` 실제 연동부터 착수할 것을 권장합니다.
