# 수정 작업 현황 (daily-news-final)

> 이 프로젝트의 **날짜별 작업 요약**입니다 (사람이 읽기 좋게 정리).
> 커밋 단위 상세 로그는 **OneDrive `수정작업현황-마스터.md`** 에 모든 프로젝트가 함께 자동 기록됩니다(집·사무실 공유).
> → 전역 git 훅이 커밋마다 마스터에 `날짜 | 작업PC | 프로젝트 | 커밋`을 추가. repo 파일(이 파일)은 훅이 건드리지 않습니다.

## 작업 PC

| PC | 호스트명 | changelog.machine |
|---|---|---|
| 🏢 사무실 | DESKTOP-UNMP5JU | 사무실 |
| 🏠 집 | (미등록) | (집 PC에서 setup 후 등록) |

**새 PC에서 자동 기록 켜기 (1회만):** OneDrive의 `changelog-setup.sh` 를 Git Bash에서 실행
```
sh ~/OneDrive/changelog-setup.sh 집      # 사무실이면 "사무실"
```

---

## 날짜별 작업 요약

### 2026-08-04 — chaovietnam 위치 모델 통일(상단/중간/하단/사이드바) + WP 위치별 자동배치
- **한 일**: chaovietnam 광고를 vnkorlife 와 동일한 위치 체계로 통일. 직원 혼란 제거 + Advanced Ads 완전 불필요.
  - 콘솔: chaovietnam 위치 선택 = 상단/중간/하단/사이드바 (기존 "기본슬롯" 하나 → 4종). 위치별 권장크기(본문 728×90, 사이드바 300×600).
  - API: 각 광고에 `slot`(=chaovietnam position) 포함. 실호출 검증(중간=in-content 반환 확인).
  - WP 플러그인 v3: 기사 본문에 **상단1 + 중간(매 2단락, 우선순위 위→아래) + 하단1** 자동 삽입. 사이드바는 `[xinchao_ad slot="sidebar"]`. 우선순위가 위치를 자동 결정.
- **배포**: daily-news Vercel(콘솔+API). WP 플러그인은 v3 재업로드 필요(사장님).
- **상태**: 🟡 3지면 배치 정교화 완료. WP v3 재업로드 후 본문 자동노출.
- **다음 단계**: 기존 광고주 이관 + Phase 4(집계·리포트).
- **관련 문서**: [PROGRESS_UNIFIED_ADS.md](PROGRESS_UNIFIED_ADS.md)

### 2026-08-03 — 통합 광고센터 Phase 3: chaovietnam 공개 API + 앱 OTA 발송
- **앱 OTA 발송완료**: 앱이 ads_unified(앱 지면) 읽도록 `eas update --channel production` rv 2.4.3 발송(chao-vn-app). 사용자 앱 재시작 시 반영.
- **공개 API 신설**: `app/api/public/ads/route.js` — 워드프레스(Firestore 직접 못 읽음)용. `GET /api/public/ads?page=` 로 chaovietnam 지면 활성광고 JSON 반환. CORS 전면 허용, 1분 캐시, 실패해도 빈 목록+200(WP 무손상).
- **배포**: 앱 OTA + daily-news Vercel(API)
- **상태**: 🟡 Phase 3 진행 — API 완료. 남은 것: WP 조각(사장님 FTP 또는 Ad Inserter 붙여넣기).
- **다음 단계**: WP 스니펫/플러그인 제공 → 사장님이 chaovietnam 에 삽입 → 라이브. 이후 Phase 4(집계·리포트).
- **관련 문서**: [PROGRESS_UNIFIED_ADS.md](PROGRESS_UNIFIED_ADS.md)

### 2026-08-03 — 통합 광고센터: 광고 규격 안내 + 이미지 자동맞춤 + 이너 우선순위
- **한 일** (실물 테스트 피드백 반영):
  - **콘솔 규격 안내**: 지면·위치별 권장 이미지 크기(📐 px) 표시. 앱/웹 규격 분리(예: 앱 head 1080×300, vnkorlife top 1456×400, chaovietnam 970×250). 업로드 영역에 "크기 달라도 자동 맞춤" 안내.
  - **자동 맞춤**: vnkorlife AdBanner 를 object-contain 으로 → 규격 안 맞는 이미지도 **잘리지 않고** 슬롯에 맞춰 표시(중립 배경).
  - **이너 우선순위**: 목록의 4개마다 이너 슬롯이 많아지는 문제 → `index` prop 도입. priority 순으로 **위 슬롯부터** 광고 1개씩 배정(모자라면 아래 슬롯 빈다).
- **배포**: vnkorlife Vercel + daily-news Vercel
- **상태**: 🟡 vnkorlife 슬롯/표시 정교화 완료.
- **다음 단계**: 앱 OTA, chaovietnam API+조각.
- **관련 문서**: [PROGRESS_UNIFIED_ADS.md](PROGRESS_UNIFIED_ADS.md)

### 2026-08-03 — 통합 광고센터 Phase 2a: vnkorlife 라이브 연결 + Firestore 규칙
- **한 일**: 통합센터에 등록한 광고가 **vnkorlife.com에 실제로 노출**되도록 연결.
  - Firestore 규칙(`chao-vn-app/firestore.rules`)에 `ads_unified`(read/write)·`ad_events`(집계) 추가 후 배포. **누락 시 콘솔 저장이 권한거부로 실패하던 것 해결.**
  - vnkorlife `AdBanner.tsx` 가 기존 `ads` 에 더해 `ads_unified`(vnkorlife 지면) 병합 로드. 인덱스 불필요, 프로덕션 빌드 통과.
  - 자체검증: 테스트 광고 write→vnkorlife 쿼리 read→delete 성공.
- **배포**: firestore rules(배포됨) + vnkorlife Vercel
- **상태**: 🟡 Phase 2a 완료. 앱(2b)·chaovietnam(3) 남음. 라이브 영향: 통합광고 등록 시 vnkorlife에 노출됨.
- **다음 단계**: Phase 2b 앱 OTA(app_ads 병행), Phase 3 워드프레스 API+조각.
- **관련 문서**: [PROGRESS_UNIFIED_ADS.md](PROGRESS_UNIFIED_ADS.md)

### 2026-08-03 — 통합 광고센터: 지면별 페이지 타겟팅 추가 (Phase 1 보강)
- **한 일**: 통합 광고 등록 폼에 **지면별 노출 페이지 선택** 추가(누락분 보강). 앱 광고 폼과 눈높이 맞춤.
  - 앱 = 기존 앱광고 폼과 동일한 페이지(메인/당근·부동산·구인 목록·상세/매거진 목록·상세/이웃사업).
  - 웹(vnkorlife+chaovietnam 공통) = **홈페이지 · 뉴스 터미날 · 상세페이지(컨텐츠+뉴스 상세 통합)** 3분류. 지정 안 하면 그 지면 전체 노출.
  - 스키마 `placements[surface].targetPages[]` 추가. 웹 읽기 매핑 규칙(상세→detail, 허브→news-terminal, 그 외→home) 문서화.
- **배포**: 웹 Vercel (git push)
- **상태**: 🟡 Phase 1 보강 완료. 라이브 광고 영향 여전히 0.
- **다음 단계**: 사장님 실물 확인 후 Phase 2(vnkorlife·앱 읽기 전환).
- **관련 문서**: [PROGRESS_UNIFIED_ADS.md](PROGRESS_UNIFIED_ADS.md)

### 2026-08-03 — 통합 자체 광고센터 Phase 1 (통합 관리 콘솔)
- **한 일**: 앱·vnkorlife.com·chaovietnam.co.kr **3개 지면의 자체 광고를 한 곳에서** 등록·관리하는 통합 콘솔 신설. 직원들이 지면마다 다른 관리처를 찾아 헤매던 문제(앱=여기 `/admin/ads`, vnkorlife=vnkorlife.com/admin 방치, chaovietnam=워드프레스 Ad Inserter)를 해결하기 위함.
  - 신규 `app/admin/ad-center/page.js` — Firestore **`ads_unified`** 컬렉션에 저장. 광고주명·지면 체크박스(앱/vnkorlife/chaovietnam)·지면별 위치·기간·우선순위·이미지/영상 업로드.
  - `app/admin/layout.js` 좌측 메뉴에 「🎯 통합 광고센터」 추가 (기존 「앱 광고 관리」는 그대로 유지).
  - 3지면이 모두 같은 Firebase 프로젝트(`chaovietnam-login`)임을 확인 → 앱·웹은 직접 읽고, 워드프레스만 API 필요.
- **배포**: 웹 Vercel (git push 자동배포)
- **상태**: 🟡 진행중 (Phase 1/4 완료). **아직 어느 지면도 `ads_unified`를 안 읽으므로 라이브 광고 영향 0** — 실제 노출은 Phase 2·3 연결 후.
- **다음 단계**: Phase 2 = vnkorlife AdBanner + 앱 FirebaseAdService가 `ads_unified` 읽도록 전환(앱은 OTA, 구버전 보호 위해 app_ads 미러링). Phase 3 = 공개 API + 워드프레스 조각. Phase 4 = 노출·클릭 집계·광고주별 리포트.
- **관련 문서**: [PROGRESS_UNIFIED_ADS.md](PROGRESS_UNIFIED_ADS.md) (전체 설계·스키마·로드맵)

### 2026-06-30 (사무실)
- **푸시 알림 내용 글자수 제한 150 → 500자 확대** — 어드민 푸시 알림 작성폼(`/admin/push-notifications`)의 내용 입력이 150자에서 잘리던 것을 500자로 늘림. "그날의 명상·짧은 에피소드"를 알림에 직접 담기 위함.
  - 원인: 150 제한은 기술적 한계가 아니라 프론트엔드 `page.js`에 손으로 박은 `slice(0,150)` 값. 서버·FCM에는 제한 없음(FCM 한계 약 4KB ≈ 한글 1,000자+).
  - 수정: `slice(0,500)` + 라벨 `/500` + textarea 3줄→6줄. 미리보기·발송 로직은 `body` 그대로 사용해 자동 반영.
  - 참고: 알림은 안드로이드 접힘 1줄/펼침 다 보임, iOS 배너 3~4줄/길게누르면 펼침. 500자 초과 콘텐츠는 링크 칸 활용 권장.

### 2026-06-08 (사무실)
- **주말 집 작업 통합** — 집에서 푸시한 11개 커밋(원격)을 사무실 로컬과 충돌 없이 병합. 항공권(Trip.com 노선별 예약)·호텔(Agoda)·Klook 맛집(추가 후 revert)·앱 market-api flights 링크·PHP 배포 전 `php -l` 검사 지침(AGENTS/CLAUDE/GEMINI) 등.
- **변경로그 방식 개선** — 훅이 repo 파일을 매번 수정해 지저분해지던 문제 해결 → 훅은 OneDrive 마스터에만 기록, repo 파일은 요약 전용으로 분리.

### 2026-06-05 (사무실)
구독자 데이터 대정비:
- **통합 리스트 병합** — 고객 통합 이메일 리스트 신규 1,147명을 구독자 DB에 추가 (이메일 기준 중복 제거)
- **바운스 정리** — 비활성/바운스 1,246건 삭제, `bounced-list.txt`(1,582건)와 대조해 재유입 차단
- **3분류 도입** — `Subscriber.category` 필드 신설(customer/general/directory) + 백필 → 고객 1,878 · 기업디렉토리 2,725 · 일반 2,582
- **손상 이메일 교정** — `.푸`(=.vn)·`.co.k`·`mailto:`·이중이메일 등 20건 정리 → 비정상 0건
- **편집창 개선** — 구독자 편집 모달에 분류 선택 추가 + 이메일 검증 한글화(브라우저 영어 에러 제거)
- 관련 스크립트: `merge-customer-list.py`, `import-customer-list.js`, `cleanup-bounced.js`, `build-classified-lists.js`, `sync-categories.js`, `clean-corrupted-emails.js`
