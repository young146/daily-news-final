# WordPress Theme Reference

이 폴더는 `chaovietnam.co.kr` 사이트의 자식 테마 파일 참조용입니다.

## ⚠️ 주의사항

- 이 파일들은 **참조용 복사본**입니다
- 실제 서버의 파일과 동기화되지 않을 수 있습니다
- 수정 시 서버에 직접 반영해야 합니다

## 📁 파일 목록

| 파일 | 설명 |
|------|------|
| `functions.php` | Sahifa 자식 테마 functions.php |

## 🔗 플러그인과의 연동

`jenny-daily-news.php` 플러그인이 사용하는 함수들:

```php
// 날씨 데이터 (10분마다 백그라운드 업데이트)
get_cached_weather_data()

// 환율 데이터 (10분마다 백그라운드 업데이트)
get_cached_exchange_rates()
```

## 🕐 Cron 작업 (서버 깨우기)

| Cron | 주기 | 역할 |
|------|------|------|
| `cron_cache_homepage_sections` | 10분 | 홈페이지 섹션 캐싱 |
| `cron_update_weather` | **매일 06:00 (VN)** | 날씨 데이터 업데이트 |
| `cron_update_exchange` | **매일 06:00 (VN)** | 환율 데이터 업데이트 |

### 2026-01-28 최적화
- 날씨/환율: 10분마다 → **하루 1회** (아침 6시 베트남 시간)
- API 호출: 하루 144회 → **1회** (대폭 감소)
- 캐시 TTL: 24시간

### 서버 Cron 설정 권장
WordPress Cron은 방문자가 있어야 실행됩니다. 안정적인 실행을 위해:
```bash
# 매일 아침 6시 (서버 시간 확인 필요)
0 6 * * * curl -s https://chaovietnam.co.kr/wp-cron.php > /dev/null
```
